flask==3.0.3
flask-cors==4.0.1
flask-limiter==3.7.0
numpy==1.26.4
pillow==10.3.0
werkzeug==3.0.3
face_recognition_models
psycopg2-binary==2.9.9
# dlib-bin and face_recognition are installed separately in Dockerfile
# to prevent pip from trying to compile dlib from C++ source
